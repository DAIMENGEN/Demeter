待办事项一：
1. webapp/src/api/common/response.ts 这个文件中的内容，是不是应该放在 webapp/src/http 目录下更合适；
2. 另外，http 目录下的文件应该需要重新组织整理一下。如果仅包含，client.ts,index.ts,request.ts 和 response.ts 的话，该如何重新组织各个文件里面的内容呢。
3. 如果，的确有一些内容，放在 client.ts,index.ts,request.ts,response.ts 中都不合适的话，应该怎么办。

请基于以上内容，给我一份方案。


待办事项二：
请告诉我现在，如果前端发送请求之后，是怎么处理响应失败的情况的，就比如后端权限验证没过，后端服务异常的情况，前端是怎么处理的，是统一处理的吗。

待办事项三：
如何把 docs/update-api-conventions.md 中关于如何设计更新接口的规范，完善到 api-design 这个skill文件内容中，并放到 skill 文件中的合适位置。
（备注：主要就是说明，更新api的传值规则，就是什么情况下，保持原有的值，什么情况下显示的清空该字段的值，什么情况下更新值等，数据类型怎么设计，数据库的字段怎么设计等，前端怎么设计，后端怎么设计，表单字段传值的时候怎么设计等，也要结合当前项目的实现，结合 update-api-conventions 中的内容，要考虑全面）
在 skill 文件中，不要写的太长太罗嗦，要简明扼要。目的就是完善 api-design 的skill，然后后续AI写出的和更新相关的功能风格都能够保持一致。